#pragma once

enum class Type{
    _undefined,
    _return,
    exit,
    _int,
    _float,
    semiColon,
    openRound,
    closeRound,
    identifier,
    openCurly,
    closeCurly,
    int_type,
    equals
};